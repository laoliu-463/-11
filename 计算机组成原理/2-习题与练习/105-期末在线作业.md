# 105-期末在线作业

> **说明**：本文件收录了用户提供的20道期末在线作业题（单选题）。
> **来源**：用户输入 "暂时保存提交作业"
> **时间**：2025-11-30

## 一、单选题 (共20题，100分)

1. (单选题) Suppose the length of two’s complement is 4 bits, which arithmetic operation brings OVERFLOW?
   （单选题） 假设二进制补码的长度为 4 位，哪种算术运算会导致溢出？
   A. 3+4
   B. (-4)+(-4)
   C. 4-(-4)
   D. 2+4

2. (单选题) The range of an (n+1)-bit integer represented in sign-magnitude format is —————.
   (单选题) 以符号-数值格式表示的 (n+1) 位整数的范围是 —————。
   A. -2ⁿ + 1 < x < 2ⁿ - 1
   B. -2ⁿ +1 ≤ x < 2ⁿ - 1
   C. -2ⁿ +1 < x ≤ 2ⁿ - 1
   D. -2ⁿ +1 ≤ x ≤ 2ⁿ - 1

3. (单选题) Given the following C program segment:
   `short si = -32767; unsigned short usi = si;`
   After executing these two statements, the value of usi is ( ).
   （单选题） 给定以下 C 程序片段：`short si = -32767; unsigned short usi = si;` 执行这两条语句后，usi 的值为 ( )。
   A. -32767
   B. 32767
   C. 32768
   D. 32769

4. (单选题) Assume signed integers are represented in two's complement. If the machine numbers of int variables x and y are FFFF FFDFH and 0000 0041H respectively, then the values of x, y, and the machine number of x - y are ————.
   （单选题） 假设有符号整数用二进制补码表示。如果整型变量 x 和 y 的机器号分别为 FFFF FFDFH 和 0000 0041H，那么 x、y 的值以及 x - y 的机器号分别为————。
   A. x = -65, y = 41, machine number of x - y overflows (x = -65，y = 41，x - y 溢出的机器编号)
   B. x = -33, y = 65, machine number of x - y is FFFF FF9DH (x = -33，y = 65，x - y 的机器编号为 FFFF FF9DH)
   C. x = -33, y = 65, machine number of x - y is FFFF FF9EH (x = -33，y = 65，x - y 的机器编号为 FFFF FF9EH)
   D. x = -65, y = 41, machine number of x - y is FFFF FF96H (x = -65，y = 41，x - y 的机器编号为 FFFF FF96H)

5. (单选题) The IEEE 754 single-precision floating-point representation:C6400000H. The value of this number is _______.
   （单选题） IEEE 754 单精度浮点数表示法：C6400000H。该数的值为_______。
   A. -1.5*2^13
   B. -1.5*2^12
   C. -0.5*2^13
   D. -0.5*2^12

6. (单选题) When the CPU executes an instruction, the address of the instruction is provided by __________.
   （单选题） 当 CPU 执行一条指令时，该指令的地址由__________提供。
   A. Program Counter (PC) 程序计数器（PC）
   B. Address code field of the instruction 指令的地址代码字段
   C. Operating System 操作系统
   D. Programmer 程序

7. (单选题) Each machine instruction, ______specifies the operation to be performed (e.g. ADD, I/O).
   （单选题） 每条机器指令，______都指定要执行的操作（例如 ADD、I/O）。
   A. Operation code 操作代码
   B. Source Operand reference 源操作数参考
   C. Result Operand reference 结果操作数参考
   D. Next Instruction Reference 下一步说明参考

8. (单选题) In stack computers, some zero-address instructions do not provide the operand addresses in the instruction ,the two operands come from ________.
   (单选题) 在栈式计算机中，一些零地址指令在指令中不提供操作数地址，两个操作数来自________。
   A. Accumulator and register 累加器和寄存器
   B. Accumulator and temporary register 累加器和临时寄存器
   C. Top and second top units of the stack 堆栈顶部和次顶部单元
   D. Top unit of the stack and a temporary register 堆栈顶部单元和一个临时寄存器

9. (单选题) A computer system uses 32-bit instructions with a 12-bit address code. If 250 two-address instructions are defined, then the number of possible one-address instructions is ().
   （单选题） 一个计算机系统使用 32 位指令和 12 位地址码。如果定义了 250 条双地址指令，那么可能的单地址指令的数量为 ()。
   A. 2¹²
   B. 2¹³
   C. 2¹
   D. 3 × 2¹³

10. (单选题) When designing an instruction system, assume a 16-bit fixed-length instruction word format is used, with the opcode using an expanding encoding scheme. The address code is 6 bits, and there are three instruction formats: zero-address, one-address, and two-address. If there are 12 two-address instructions and 254 one-address instructions, then the maximum number of zero-address instructions is _________.
    A. 0
    B. 2
    C. 64
    D. 128

11. (单选题) A computer uses a 16-bit fixed-length instruction word format. The number of bits for the opcode and the addressing mode is fixed. The instruction set has 48 instructions and supports four addressing modes: direct, indirect, immediate, and relative. In a single-address instruction, the addressable range using direct addressing is _________.
    A. 0–255
    B. 0–1023
    C. -128–127
    D. -512–511

12. (单选题) Among the following addressing modes, which one is most suitable for accessing elements of an array in sequential order by index(下标)?
    A. Relative addressing
    B. Register addressing
    C. Direct addressing
    D. Indexed addressing

13. (单选题) Among the instruction addressing modes, the fastest way to obtain an operand is __________.
    A. Direct addressing
    B. Immediate addressing
    C. Register addressing
    D. Indirect addressing

14. (单选题) Assuming the address code in the instruction provides the effective address of the operand, then this instruction uses _________.
    A. Direct addressing
    B. Immediate addressing
    C. Register addressing
    D. Indirect addressing

15. (单选题) Assume the value in register R is 200. The contents of memory address units 200 and 300 are 300 and 400, respectively. Under which addressing mode will the operand accessed be 200? ( )
    A. Direct addressing
    B. Register indirect addressing
    C. Memory indirect addressing
    D. Register addressing

16. (单选题) The instruction pipeline divides an instruction into three stages: fetch, decode, and execute,and the time for each stage is Δt. If 12 instructions are executed，the total time required is __________.
    A. 12Δt
    B. 14Δt
    C. 16Δt
    D. 18Δt

17. (单选题) The aim of the indirect cycle is to get _________________.
    A. an operand
    B. an instruction
    C. an address of an instruction
    D. the address of an operand

18. (单选题) Which is NOT a characteristic of RISC processor? ______.
    A. indirect addressing mode
    B. register to register operations
    C. a single instruction size
    D. simple instruction format

19. (单选题) The correct instruction cycle sequence is _________________ .
    A. Fetch , Indirect , Execute and Interrupt
    B. Fetch , Execute , Indirect and Interrupt
    C. Fetch , Indirect , Interrupt and Execute
    D. Indirect , Fetch , Execute and Interrupt

20. (单选题) At the end of _________ , the content of MBR is instruction.
    A. indirect sub-cycle
    B. fetch sub-cycle
    C. execute sub-cycle
    D. interrupt sub-cycle
