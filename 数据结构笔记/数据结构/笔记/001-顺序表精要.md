# 顺序表精要

> 线性表的顺序存储实现核心知识  
> 最后更新：2025年1月

## 📚 相关：[思维导图](./数据结构思维导图.md) | [代码练习](../代码练习/01-顺序表/) | [单链表](./002-单链表精要.md)

---

## 一、基本概念

### 什么是顺序表？

**通俗理解：** 顺序表就像"一排连续的座位"，数据元素按照顺序依次存储在连续的内存空间中，就像电影院的座位，每个座位都有固定的位置编号。

### 核心特点

```
┌─────────────────────────────────────────────────────────┐
│                   顺序表存储结构                         │
└─────────────────────────────────────────────────────────┘

内存地址:  1000  1004  1008  1012  1016  1020  1024
          ┌────┬────┬────┬────┬────┬────┬────┐
          │ a₁ │ a₂ │ a₃ │ a₄ │ a₅ │    │    │
          └────┴────┴────┴────┴────┴────┴────┘
索引:       [0]  [1]  [2]  [3]  [4]  空闲  空闲
          ▲                           ▲      ▲
        首地址                    当前长度   容量
```

**关键特性：**
- ✅ **连续存储**：所有元素存储在连续的内存空间中
- ✅ **随机存取**：可以通过下标直接访问任意位置的元素，时间复杂度 O(1)
- ✅ **存储密度高**：只需存储数据本身，不需要额外的指针空间
- ⚠️ **插入删除慢**：需要移动大量元素，时间复杂度 O(n)
- ⚠️ **需要预分配空间**：容量固定，可能浪费空间或溢出

---

## 二、存储结构

### C语言实现

```c
#define MAXSIZE 100
#define OK 1
#define ERROR 0
#define TRUE 1
#define FALSE 0

typedef int ElemType;  // 假设元素类型为int
typedef int Status;    // 函数返回状态

typedef struct {
    ElemType data[MAXSIZE];  // 存储数据元素的数组
    int length;              // 当前长度
} SqList;
```

**结构说明：**
- `data[]`：数组，存储数据元素
- `length`：当前顺序表中实际元素的个数
- `MAXSIZE`：顺序表的最大容量（常量）

**内存布局：**
**内存布局：**
```
SqList 结构体在内存中的布局（栈上分配为例）：

┌──────────────────────────────────────────────┐
│  SqList 结构体                               │
│  ┌────────────────────────────────────────┐  │
│  │ data[0] data[1] ... data[99]           │  │ 400字节 (100 * 4)
│  └────────────────────────────────────────┘  │
│  ┌──────────┐                                │
│  │ length   │                                │ 4字节
│  └──────────┘                                │
└──────────────────────────────────────────────┘
总大小: 400 + 4 = 404 字节 (连续内存)
```

---

## 三、基本操作

### 1. 初始化

**功能：** 创建一个空的顺序表

```c
// 初始化顺序表
Status InitList(SqList *L) {
    L->length = 0;  // 初始长度为0
    return OK;
}
```

**时间复杂度：** O(1)  
**空间复杂度：** O(1)

**示例：**
```c
SqList L;
InitList(&L);  // L.length = 0
```

---

### 2. 插入操作

**功能：** 在指定位置插入新元素

**算法步骤：**
1. 检查插入位置是否合法（1 ≤ i ≤ length+1）
2. 检查顺序表是否已满
3. 将第 i 个位置及之后的所有元素后移一位
4. 将新元素插入到第 i 个位置
5. 长度加1

```c
Status ListInsert(SqList *L, int i, ElemType e) {
    // 1. 检查插入位置合法性
    if (i < 1 || i > L->length + 1) {
        return ERROR;  // 插入位置不合法
    }
    
    // 2. 检查顺序表是否已满
    if (L->length >= MAXSIZE) {
        return ERROR;  // 顺序表已满
    }
    
    // 3. 将第 i 个位置及之后的元素后移
    for (int j = L->length; j >= i; j--) {
        L->data[j] = L->data[j - 1];
    }
    
    // 4. 插入新元素
    L->data[i - 1] = e;
    
    // 5. 长度加1
    L->length++;
    
    return OK;  // 插入成功
}
```

**插入过程图示：**

```
插入前：在位置3插入元素 99
┌────┬────┬────┬────┬────┐
│ 10 │ 20 │ 30 │ 40 │ 50 │
└────┴────┴────┴────┴────┘
 [0]  [1]  [2]  [3]  [4]
length = 5

步骤1：元素后移
┌────┬────┬────┬────┬────┬────┐
│ 10 │ 20 │ 30 │ 40 │ 50 │ 50 │  ← data[4] → data[5]
└────┴────┴────┴────┴────┴────┘
           ↓
┌────┬────┬────┬────┬────┬────┐
│ 10 │ 20 │ 30 │ 40 │ 40 │ 50 │  ← data[3] → data[4]
└────┴────┴────┴────┴────┴────┘
           ↓
┌────┬────┬────┬────┬────┬────┐
│ 10 │ 20 │ 30 │ 30 │ 40 │ 50 │  ← data[2] → data[3]
└────┴────┴────┴────┴────┴────┘

步骤2：插入新元素
┌────┬────┬────┬────┬────┬────┐
│ 10 │ 20 │ 99 │ 30 │ 40 │ 50 │  ← data[2] = 99
└────┴────┴────┴────┴────┴────┘
 [0]  [1]  [2]  [3]  [4]  [5]
length = 6
```

**时间复杂度分析：**
- **最好情况：** 在表尾插入（i = length+1），无需移动元素，O(1)
- **最坏情况：** 在表头插入（i = 1），需要移动所有元素，O(n)
- **平均情况：** 等概率插入，平均移动 n/2 个元素，O(n)

**空间复杂度：** O(1)

---

### 3. 删除操作

**功能：** 删除指定位置的元素

**算法步骤：**
1. 检查删除位置是否合法（1 ≤ i ≤ length）
2. 保存要删除的元素（可选）
3. 将第 i+1 个位置及之后的所有元素前移一位
4. 长度减1

```c
Status ListDelete(SqList *L, int i, ElemType *e) {
    // 1. 检查删除位置合法性
    if (i < 1 || i > L->length) {
        return ERROR;  // 删除位置不合法
    }
    
    // 2. 保存要删除的元素
    *e = L->data[i - 1];
    
    // 3. 将第 i+1 个位置及之后的元素前移
    for (int j = i; j < L->length; j++) {
        L->data[j - 1] = L->data[j];
    }
    
    // 4. 长度减1
    L->length--;
    
    return OK;
}
```

**删除过程图示：**

```
删除前：删除位置3的元素
┌────┬────┬────┬────┬────┐
│ 10 │ 20 │ 30 │ 40 │ 50 │
└────┴────┴────┴────┴────┘
 [0]  [1]  [2]  [3]  [4]
length = 5

步骤1：元素前移
┌────┬────┬────┬────┬────┐
│ 10 │ 20 │ 40 │ 40 │ 50 │  ← data[3] → data[2]
└────┴────┴────┴────┴────┘
           ↓
┌────┬────┬────┬────┬────┐
│ 10 │ 20 │ 40 │ 50 │ 50 │  ← data[4] → data[3]
└────┴────┴────┴────┴────┘

步骤2：长度减1
┌────┬────┬────┬────┬────┐
│ 10 │ 20 │ 40 │ 50 │    │
└────┴────┴────┴────┴────┘
 [0]  [1]  [2]  [3]  [4]
length = 4
```

**时间复杂度分析：**
- **最好情况：** 删除表尾元素（i = length），无需移动元素，O(1)
- **最坏情况：** 删除表头元素（i = 1），需要移动 n-1 个元素，O(n)
- **平均情况：** 等概率删除，平均移动 (n-1)/2 个元素，O(n)

**空间复杂度：** O(1)

---

### 4. 查找操作

#### 4.1 按位置查找

**功能：** 查找第 i 个位置的元素

```c
Status GetElem(SqList L, int i, ElemType *e) {
    if (i < 1 || i > L.length) {
        return ERROR;  // 位置不合法
    }
    *e = L.data[i - 1];  // 数组下标从0开始
    return OK;
}
```

**时间复杂度：** O(1) - 随机存取  
**空间复杂度：** O(1)

#### 4.2 按值查找

**功能：** 查找值为 e 的元素位置

```c
int LocateElem(SqList L, ElemType e) {
    for (int i = 0; i < L.length; i++) {
        if (L.data[i] == e) {
            return i + 1;  // 返回位置（从1开始）
        }
    }
    return 0;  // 未找到
}
```

**时间复杂度分析：**
- **最好情况：** 目标元素在表头，O(1)
- **最坏情况：** 目标元素在表尾或不存在，O(n)
- **平均情况：** 等概率查找，平均比较 n/2 次，O(n)

**空间复杂度：** O(1)

---

### 5. 遍历操作

**功能：** 访问顺序表中的所有元素

```c
void PrintList(SqList L) {
    printf("顺序表内容：");
    for (int i = 0; i < L.length; i++) {
        printf("%d ", L.data[i]);
    }
    printf("\n");
}
```

**时间复杂度：** O(n)  
**空间复杂度：** O(1)

---

### 6. 判空操作

**功能：** 判断顺序表是否为空

```c
Status ListEmpty(SqList L) {
    if (L.length == 0) return TRUE;
    else return FALSE;
}
```

**时间复杂度：** O(1)  
**空间复杂度：** O(1)

---

### 7. 获取长度

**功能：** 获取顺序表的当前长度

```c
int ListLength(SqList L) {
    return L.length;
}
```

**时间复杂度：** O(1)  
**空间复杂度：** O(1)

---

## 四、时间复杂度总结

| 操作 | 最好情况 | 平均情况 | 最坏情况 | 说明 |
|------|---------|---------|---------|------|
| **插入** | O(1) | O(n) | O(n) | 表尾插入最快 |
| **删除** | O(1) | O(n) | O(n) | 表尾删除最快 |
| **按位置查找** | O(1) | O(1) | O(1) | 随机存取 |
| **按值查找** | O(1) | O(n) | O(n) | 需要遍历 |
| **遍历** | O(n) | O(n) | O(n) | 必须访问所有元素 |
| **判空** | O(1) | O(1) | O(1) | 常数时间 |
| **获取长度** | O(1) | O(1) | O(1) | 常数时间 |

---

## 五、空间复杂度

**总空间复杂度：** O(n)

**空间组成：**
- 数据存储：O(n) - 存储 n 个元素
- 辅助空间：O(1) - 只需几个变量

**存储密度：** 100%（仅存储数据，无额外指针）

---

## 六、优缺点分析

### ✅ 优点

1. **随机存取**：可以通过下标直接访问任意位置的元素，时间复杂度 O(1)
2. **存储密度高**：只需存储数据本身，不需要额外的指针空间
3. **实现简单**：使用数组即可实现，代码简洁
4. **缓存友好**：连续存储，访问局部性好

### ❌ 缺点

1. **插入删除慢**：需要移动大量元素，时间复杂度 O(n)
2. **需要预分配空间**：容量固定，可能浪费空间或溢出
3. **动态扩展困难**：需要重新分配更大的内存空间并复制数据

---

## 七、应用场景

### 适合使用顺序表的场景

1. **频繁查询、很少插入删除**：如学生成绩表、商品信息表
2. **数据量固定或变化不大**：如月份数据、星期数据
3. **需要随机访问**：如数组索引、哈希表冲突处理
4. **存储密度要求高**：内存受限的嵌入式系统

### 不适合使用顺序表的场景

1. **频繁插入删除**：适合使用链表
2. **数据量变化大**：适合使用动态数组或链表
3. **需要频繁在中间位置操作**：适合使用链表

---

## 八、完整代码示例

```c
#include <stdio.h>
#include <stdlib.h>

#define MAXSIZE 100
#define OK 1
#define ERROR 0
#define TRUE 1
#define FALSE 0

typedef int ElemType;
typedef int Status;

typedef struct {
    ElemType data[MAXSIZE];
    int length;
} SqList;

// 初始化
Status InitList(SqList *L) {
    L->length = 0;
    return OK;
}

// 插入
Status ListInsert(SqList *L, int i, ElemType e) {
    if (L->length >= MAXSIZE) return ERROR;
    if (i < 1 || i > L->length + 1) return ERROR;
    for (int j = L->length; j >= i; j--) {
        L->data[j] = L->data[j-1];
    }
    L->data[i-1] = e;
    L->length++;
    return OK;
}

// 删除
Status ListDelete(SqList *L, int i, ElemType *e) {
    if (L->length == 0) return ERROR;
    if (i < 1 || i > L->length) return ERROR;
    *e = L->data[i-1];
    for (int j = i; j < L->length; j++) {
        L->data[j-1] = L->data[j];
    }
    L->length--;
    return OK;
}

// 按位置查找
Status GetElem(SqList L, int i, ElemType *e) {
    if (L.length == 0 || i < 1 || i > L.length) return ERROR;
    *e = L.data[i-1];
    return OK;
}

// 按值查找
int LocateElem(SqList L, ElemType e) {
    for (int i = 0; i < L.length; i++) {
        if (L.data[i] == e) return i+1;
    }
    return 0;
}

// 遍历
void PrintList(SqList L) {
    printf("顺序表：");
    for (int i = 0; i < L.length; i++) {
        printf("%d ", L.data[i]);
    }
    printf("\n");
}

int main() {
    SqList L;
    ElemType e;
    
    InitList(&L);
    
    // 插入元素
    ListInsert(&L, 1, 10);
    ListInsert(&L, 2, 20);
    ListInsert(&L, 3, 30);
    PrintList(L);  // 输出：顺序表：10 20 30
    
    // 在位置2插入99
    ListInsert(&L, 2, 99);
    PrintList(L);  // 输出：顺序表：10 99 20 30
    
    // 删除位置3的元素
    if (ListDelete(&L, 3, &e)) {
        printf("删除的元素：%d\n", e);  // 输出：删除的元素：20
    }
    PrintList(L);  // 输出：顺序表：10 99 30
    
    return 0;
}
```

---

## 九、常见考点

### 1. 插入操作的移动次数

**问题：** 在长度为 n 的顺序表中，在第 i 个位置插入一个元素，需要移动多少个元素？

**答案：** n - i + 1 个元素

**分析：**
- 第 i 个位置及之后的所有元素都需要后移
- 需要移动的元素：data[i-1] 到 data[n-1]，共 n - (i-1) = n - i + 1 个

### 2. 删除操作的移动次数

**问题：** 在长度为 n 的顺序表中，删除第 i 个位置的元素，需要移动多少个元素？

**答案：** n - i 个元素

**分析：**
- 第 i+1 个位置及之后的所有元素都需要前移
- 需要移动的元素：data[i] 到 data[n-1]，共 n - i 个

### 3. 平均移动次数

**插入操作的平均移动次数：**
- 假设在 n+1 个位置等概率插入
- 平均移动次数 = (0 + 1 + 2 + ... + n) / (n+1) = n/2

**删除操作的平均移动次数：**
- 假设在 n 个位置等概率删除
- 平均移动次数 = (0 + 1 + 2 + ... + (n-1)) / n = (n-1)/2

---

## 十、与其他数据结构的对比

### 顺序表 vs 链表

| 特性 | 顺序表 | 链表 |
|------|--------|------|
| **存储方式** | 连续存储 | 链式存储 |
| **随机存取** | ✅ O(1) | ❌ O(n) |
| **插入删除** | ❌ O(n) | ✅ O(1) |
| **存储密度** | ✅ 高 | ❌ 低（需要指针） |
| **内存要求** | 需要连续空间 | 可以分散存储 |
| **动态扩展** | ❌ 困难 | ✅ 容易 |

### 顺序表 vs 动态数组 (C语言 malloc)

| 特性 | 静态顺序表 (数组) | 动态顺序表 (malloc) |
|------|--------|---------------------|
| **容量** | 编译时固定 (MAXSIZE) | 运行时可调整 (realloc) |
| **内存位置** | 栈 (Stack) 或 全局区 | 堆 (Heap) |
| **生命周期** | 函数结束自动释放 | 需手动 free 释放 |
| **灵活性** | 低，易溢出或浪费 | 高，按需分配 |

---

## 十一、学习要点总结

### ⭐⭐⭐ 必须掌握

1. **顺序表的定义和存储结构**
2. **插入和删除操作的实现和复杂度分析**
3. **查找操作的实现（按位置、按值）**
4. **时间复杂度分析（最好、平均、最坏）**

### ⭐⭐ 重要理解

1. **顺序表的优缺点及应用场景**
2. **插入删除操作的元素移动次数计算**
3. **与链表的对比**

### ⭐ 了解内容

1. **动态扩展的实现方法**
2. **实际应用中的优化技巧**

---

## 总结

```
✅ 顺序表 = 连续存储 + 随机存取 + 高存储密度
✅ 适合：频繁查询、数据量固定、需要随机访问
❌ 不适合：频繁插入删除、数据量变化大
✅ 核心操作时间复杂度：插入O(n)、删除O(n)、查找O(1)/O(n)
```

---

**精简要点**: 顺序表是线性表的最基本实现方式，理解其连续存储和随机存取特性是学习数据结构的基础 ⚙️✨

